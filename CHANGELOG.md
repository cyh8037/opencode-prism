# Changelog

本文件记录每个发布版本中**用户可感知**的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **`/bg`、`/split` 任务描述形式改为插件原生直接执行**：输入命令后由插件完成启动与调度（模型回合只剩转达确定性回执），消除"模型读模板后不调工具、自己开干"的不确定性（沙箱实测复现过的失败模式），并把命令链路从"模板回合 + 工具调用回合"压缩为单回合。`/split` 的意图判定与规划在插件管理的一次性子会话中异步进行，计划/意图结论/失败原因自动注入会话（dry-run 计划包代码围栏、注入后等用户确认再执行）；`/bg --parallel N` 是唯一保留的模型分支（语义拆分需 LLM，经注入的【并行启动】指令驱动）。新会话**第一条消息**就用 `/bg` 且未配置默认 `model` 时会报"无法确定主会话的当前模型"（模型继承三级回退链的既定语义），会话发生任意一轮回合后重试即自愈。
- **状态看板改 markdown 管道表格（修复 web 端错位）**：`/bg status`、`/split status`、单任务看板与完成通知中的看板从 box-drawing 字符表格改为 GFM 管道表格（`| 列 | 列 |`）——TUI 等宽终端按字符对齐，web 前端由 GFM 解析为 HTML 表格，不依赖字体比例（web 端代码块字体 CJK≈1.67×ASCII，box-drawing 表格含中文必错位，2026-08-29 像素级实测确认）。看板不再包 ` ```text ` 围栏（围栏会把表格降级为代码块、错位复现）；纯分层缩进文本（dry-run 计划、`/split status sp_xxx` 明细）保留围栏保形。完成通知的转述指令同步更新为"保留表格的 | 列分隔结构"。
- **TUI 环境探测与文案门控**：按 `client.tui` 运行时面探测（`isTuiClient`），非 TUI 环境（web/headless）下子会话导航指引（leader 键+↓）自动替换为 `/bg status`、`bg_output` 等工具侧等价查看方式——`bg_spawn` 回执、命令模板与看板注释行三处生效。探测信号在 web 会话的实际表现待人工验证，失败时回退为现状显示。
- **子会话实时查看（原生 TUI 导航引导）**：每个后台子会话都是真实会话，自动进入 TUI 的子会话导航组——运行中按 leader 键（默认 Ctrl+X）+ ↓ 进入子会话**实时视图**（输出流式滚动），←/→ 切换，↑ 返回主会话，完成后历史仍可回看。子会话标题改为 `[bg_ 任务id] 描述 (prism)`（描述经控制字符清洗与截断），←/→ 切换时可与任务 id 对号。`bg_spawn` 返回文本、`/bg status` 看板与 `/bg`、`/split` 命令模板均附导航指引。
- **`split.intentCheck` 拆分意图识别（默认 `true`）**：`/split` 与 `split_task` 拆分前先用主会话模型做意图判定——简单任务返回**无需拆分**及原因（不再强行产出 2 个以上子任务），复杂任务照常规划；`--dry-run` 同样先判定（消息注明预览判定）。判定超时/失败不阻塞拆分（fail-open 继续原流程），判定子会话 30s 超时、不重试、工具面禁用。设为 `false` 可跳过判定，省去这一次额外模型调用。
- **`split.autoTrigger` 自主触发拆分（默认 `true`）**：`split_task` 工具描述拼接自主触发准则——主模型在普通对话中（无 `/split`）遇到 3 个以上可识别的独立子工作、多模块并行改动、调研+实现类复合任务时可主动拆分执行并立即告知用户；单步任务、需实时确认的交互、破坏性操作等明确排除。默认与 `intentCheck` 同开：自主触发的拆分同样被意图判定兜底防误拆。
- **`/bg status` 与 `/split status` 看板化**：后台任务列表升级为纯文本对齐看板（无 Emoji，任何终端下表格严格对齐）——`/bg status` 展示运行/排队计数、并发池占用、时长与工具调用进度；`/split status` 按依赖分层展示子任务 DAG（COMPLETED / RUNNING / BLOCKED / SKIPPED / ARCHIVED 全状态），并保留不属于任何拆分运行的后台任务区块。完成通知同步看板化（错误信息保留在状态单元格内）。`bg_wait` / `bg_output` 工具返回保持紧凑文本不变。
- **`background.autoTrigger` 配置（策略 A）**：默认 `true`——`bg_spawn` 工具描述提示模型在耗时只读调研、独立编译/测试/压测、相互独立的子模块任务等场景下**主动调用**后台任务（无需用户显式要求），启动后立即告知用户；需要用户实时确认、与主会话编辑同一批文件、破坏性操作等场景明确排除。`false` 回退到仅按用户显式要求启动。插件加载时读取，切换需重启 opencode。
- **`/bg` 图片任务自动传图**：`bg_spawn` 执行时自动把父会话**最后一条用户消息**的图片附件传给后台子会话（复用视觉任务的传图通道与批量上限 4 张）——`/bg 分析这张图` 现在可以真正后台化，子任务用 `vision_look` 读图，不再要求图片必须有本地路径。基于早前消息的图片开新后台任务时，把该图片的本地路径/URL 写进 prompt（自动传图只跟随当前消息，避免把旧图误注入不相干任务）。`vision.enabled: false` 时不传图（视觉完全关闭，子会话没有 `vision_look`）。
- **`/bg`、`/split` 图片命令提醒覆盖**：无视觉模型收到图片报错时，原有的"请调用 vision_look"提醒会把 `/bg`、`/split` 命令回合带偏（模型直接同步解读，放弃用户显式要求的后台/拆分执行）。现在命令回合注入专用提醒：`/bg` 引导调用 `bg_spawn`（图片自动跟随），`/split` 引导调用 `split_task` 并把图片路径写进 task 描述（split 子任务没有图片通道），均明确"不要用 vision_look 同步解读"。
- **`status` 变体容错**：`/bg status`、`/split status` 前缀命中但变体未识别（如把 `--all` 敲成 `--al`）时给出用法提示，不再穿透成任务描述被当成新任务启动。

### 修复与改进

- **`/split cancel`（不带参数）恢复取消全部**：命令原生执行重构中该分支被误删，裸 `cancel` 会穿透成任务描述被 spawn 成名为 "cancel" 的拆分任务——现恢复为取消当前会话全部后台任务（与 `/bg cancel` 对称），并新增 cancel 前缀拦截：`cancel --all` 等未识别变体给用法提示（不再报"任务不存在: --all"或被当任务启动）。
- **`/bg` 入队回执操作指引顺序修正**：原生命令（`/bg output <id>`、`/bg cancel <id>`）前置，`bg_output` / `bg_cancel` 工具名降为补充——用户最直接可用的操作不再被埋在后半句。
- **`/bg --parallel N` 用户反馈**：注入内容增加"已交给模型拆分（N=x）"前置反馈，模型指令后置——用户明确知道该分支走了不同的执行路径，不再只看到一段模型指令。
- **转达指令强化为"完整转达"**：命令模板从"原样转达"改为"完整转达（含操作指引行，不要省略、压缩或重排）"——沙箱实测发现模型转达回执时只保留第一行、裁剪操作指引与导航行（用户因此看不到如何查看/取消），强化后实测三行逐字完整转达。
- **启动失败提示可行动化**：`/bg` 启动失败注入文案区分"插件正在关闭"与一般失败（附"稍后重试或检查模型配置"引导），不再裸透传 raw error。
- **`/split` 回执预期管理**：任务描述回执补充"通常需十几秒，失败会自动提示"——dry-run/意图判定的异步等待不再是静默窗口。

### 修复与改进

- **外部数据解析 zod 化**：SDK 返回值、事件 payload、消息历史的约 6 处手写校验收敛为共享的宽容语义 zod schema（新增 `shared/session-data.ts`：消息信封、事件 sessionID、模型引用、状态表）——删除跨模块重复实现与 `as` 强转，行为对既有输入逐用例等价。一处加固：`session.status()` 返回表此前弱检查后强转使用，坏条目会被当作 idle（有误完成任务的窗口），现校验失败按 fail-closed 处理（本轮 sweep 跳过/完成确认推迟）。
- **终态 toast 分级摘要与防刷屏**：批量任务（`/split`、`--parallel`）收尾 toast 从"最后一个任务的状态"（混合批次末尾会误报绿色成功）改为**整批计数摘要**——`全部后台任务已结束: N 成功, M 失败[, K 取消]`，variant 按整体结果定级（有失败红、仅取消黄、全成绿）；批内失败 toast 追加错误原因摘要（80 字符清洗截断），8 秒窗口内连续失败自动合并；`/split cancel sp_xxx` 从逐任务连弹 N 条 CANCELLED 改为单条汇总（与全量取消语义一致）；`showToast` variant 参数收窄为联合类型；`/split` 启动回执文案修正（进度经 TUI 子会话导航与 `/split status` 查看，不再声称"通过 toast 展示"）。
- **子会话工具面收紧**：后台/拆分子会话的工具过滤新增禁用 `split_task`——此前子会话保留完整拆分入口，配合自主触发准则存在嵌套拆分面（嵌套运行注册在子会话名下，主会话 `/split status` 不可见且绕开熔断预算）；规划器与意图识别的一次性子会话同样禁用全部 Prism 工具（`bg_*` / `split_task` / `vision_look` / `question`）。
- **完成通知结果采集加固**：任务完成的"完整结果"一律以子会话消息历史的 assistant 文本为准（始终覆盖事件路径的预览值）——此前用户经 TUI 切进子会话输入的消息会被事件路径捕获，存在被当"完整结果"注入主会话的窗口。
- **`/bg status` 看板标题修复**：并发池信息从标题行移出（此前标题含 Pool 段时会超宽截断，截断点常落在模型名中间，观感像文本损坏），现在标题只含运行/排队计数（必不超宽），Pool 在表格下方独立行完整显示。
- **看板结果原样转达**：`/bg status` 与 `/split status` 注入的看板要求模型**原样转达**——不再改写为列表、不加 emoji、不合并行（此前模型常把表格转述成带 emoji 的 markdown 列表，丢失依赖信息与表格形态）。
- **`/split status` 展示精简**：波头文案从 39 字精简为 `Wave N (依赖前一波,依赖满足即启动)`（多层看板不再每层重复超长文案、行宽降回 ~80 列避免 TUI 折行）；去掉每行的 `<- 无依赖` 冗余；层内任务统一 `[id]` 缩进（不再用 `+` 续行标记，减少模型转述变形空间）。
- **split 运行 id 与按 run 取消**：每个 `/split` 运行自动分配 `sp_xxxxxxxx` id（显示在 `/split status` 看板标题）——`/split cancel sp_xxx` 可**取消整个拆分运行**（遍历其子任务，终态自动跳过，下游级联 SKIPPED），不再需要按子任务逐个取消或全量取消。
- **看板分区折叠**：`/bg status` 与 `/split status` 默认只展示进行中的任务/运行——已结束（COMPLETED/ERROR/CANCELLED/SKIPPED）折叠为一行摘要（`+ N 已结束: 1 CANCELLED, 5 SKIPPED`），取消后主视图立即干净但因果可追溯。`/bg status --all`、`/split status --all` 展开全部；`/split status sp_xxx` 查看单个 run 的完整 DAG 明细；`/bg status bg_xxx` 查看单个任务的表格视图（无论是否已结束，展示当前状态）。
- **看板单元格净化加固**：ANSI 转义序列整序列剥离（此前只删 ESC 字节会残留 `[31m` 之类的可见尾巴，破坏对齐观感），控制字符剥离范围显式转义书写。
- **后台子任务恢复调用 `vision_look` 读图**：0.4.0-beta.1 的递归防护把"解读子会话"误扩大为"所有 prism 子会话"，普通后台子任务（`/bg`、`/split` 子任务）调用 `vision_look` 一律被拒。现在只有真正携带注入图片的解读上下文（同步解读子会话、异步视觉任务）才拒绝嵌套解读；普通子任务可对自有图片调用 `vision_look` 读图（解读子会话的递归防护不变）。
- **`bg_send` 补充指令上限**：单个任务未投递的补充指令超过 10 条时拒绝新消息（报错提示等待下一轮投递），单条消息超过 32KB 自动截断并记录日志——队列消息最终会原样注入子会话 prompt，此前无上限可致内存膨胀与上下文溢出。
- **远程图片流式下载**：无 `Content-Length` 的 chunked 响应此前会被整体读入内存后才做大小检查，超大（或恶意）响应可致内存占用失控；现在按块流式读取，超过单图上限（4MB）立即中断下载并丢弃。

## [0.4.0-beta.1] - 2026-08-26

### 修复

- **`vision_look` 解读子会话递归风暴**：解读子会话继承全量工具集，子会话模型（无法读图时）可能调用 `vision_look` 解读自己收到的图片，无限生成解读子会话（实测 60 秒 26+ 个）。修复：解读子会话禁用全部 Prism 工具（`tools: {vision_look: false, ...}`）；`vision_look` 拒绝在解读子会话内嵌套执行。
- **`vision_look` 参数兼容小模型**：`images` 支持 `string | string[]`（小模型常把 `["last"]` 输出为 `"last"`）；`"last"` 哨兵大小写不敏感且容忍首尾空格；`["last", 路径]` 混用时不再把 `"last"` 当文件路径导致 `ENOENT`，而是以 `"last"` 优先并注明忽略的显式引用。

## [0.4.0-beta.0] - 2026-08-23

### 破坏性变更

- **`vision.mode` 的 `"background"` 值改名为 `"async"`**：旧值不再被接受，该字段单独回退默认 `"sync"`（配置校验按字段回退，同节的 `vision.model` / `vision.tools` 等有效设置保留），请更新配置。`"sync"` 不变。
- **`vision.tools: []` 语义修正**：此前空数组与不配置无区别（都检查所有工具输出），现在 `[]` = 不触发任何自动解读；缺省仍为所有工具都检查。

### 新增

- **`vision.enabled` 总开关（默认 `true`）**：`false` 完全关闭视觉功能——`vision_look` 工具不注册、自动解读不触发、子会话工具列表也移除 `vision_look`（`bg_spawn` 工具描述与 `/bg`、`/split` 命令模板里的读图指引随之停用，子任务不会被指引去调用已移除的工具）。显式关闭优先于 `vision.model` 配置；改动需重启 opencode 生效。

## [0.3.0-beta.0] - 2026-08-23

### 破坏性变更

- **移除 `/vision` 命令**：视觉解读收敛为两个入口——工具输出自动解读（`vision.mode` 控制同步/后台）与 `vision_look` 工具（`"last"` 哨兵、`goal` 关注点、`[Image N]` 占位符处理全部保留）。习惯用 `/vision` 的用户：请让主模型调用 `vision_look`，或依赖工具输出的自动解读；对话贴图场景主模型会自动调用 `vision_look(["last"])`。
- **`/split` 任务模式改由主模型调用 `split_task` 工具执行**：此前任务描述在 `command.execute.before` hook 里同步跑完整规划器 LLM 往返（实测约 11s），期间 TUI 完全无反馈，如死机。现在命令输入后立即流式响应，规划在工具执行中完成；status / output / cancel 子命令仍由插件原生执行。`split.tool` 语义随之收紧：`false` 现在同时关闭 `split_task` 工具与 `/split` 命令注册（此前仅关闭工具入口）。

### 新增

- **`split_task` 工具（模型自动拆分）**：主会话模型可自主调用，触发与 `/split` 完全相同的管线（规划器拆分 → 依赖调度 → 完成后汇总回注），复杂任务不再需要用户手动输入命令。参数 `task` / `dry_run` / `sequential` / `max` 与命令旗标一一对应（`max` 在服务入口统一钳制到 2-12，命令与工具两条入口共享同一份边界逻辑）。经 `split.tool` 配置开关控制（默认开启，`{ "split": { "tool": false } }` 关闭；开关同时门控工具与 `/split` 命令，改动需重启 opencode 生效）。
- **子任务不活跃看门狗**：运行中的后台任务若 30 分钟内无任何输出活动（疑似挂起——卡死的模型调用或死工具，这类静默失败工具断路器与 TTL 告警都覆盖不到），自动取消并在通知与汇总报告中标注原因，下游依赖任务级联跳过。任何消息活动（含 steering 投递与续跑）都会重置计时；仅作用于运行中任务，排队任务不受影响。
- **`/split --dry-run` 按波次展示计划**：拆分计划按依赖拓扑分层渲染（"第 1 波（无依赖，立即启动）/ 第 2 波（依赖在前一波；各任务在其依赖完成后即启动，不等整波全部结束）…"），如实反映运行时的 ASAP 依赖驱动调度；`--sequential` 改按实际执行顺序编号展示。
- **`bg_send` 工具与 `/bg send <id> <补充指令>`（中途投递 steering）**：向运行中的后台任务发送补充指令——消息排队，在子会话当前回合结束的边界合并为一次补充回合投递，不打断执行、完整保留子会话上下文（对已结束任务等同续跑；`/bg resume` 对运行中任务也改为排队投递，不再报错；排队中任务的补充指令直接并入首轮启动 prompt）。完成判定带时序保护（投递后 8s 宽限 + 判定前新鲜状态复查），轮询的过期状态快照不会把刚接受补充回合的任务误判完成；**已承诺"排队"的消息绝不静默丢失**——结算确认窗口内新到达的消息会让完成让位给下一个回合边界，投递等待期间任务进入同模型重试时在途消息重新入队（随重启并入首轮或下一边界投递，仅终态丢弃）。投递失败自动重试（上限 3 次，不影响期间新排队的消息），成功投递重置该回合的工具调用预算与 TTL 锚点；`bg_output` 与 `/bg status` 显示排队中的消息数。
- **`bg_wait` 工具**：阻塞等待指定任务（缺省 = 当前会话全部未结束任务；显式空数组 = 无事可等，立即返回）到达终态，默认 120s、上限 600s，超时返回当前状态表。模型在总结前等待并行任务完成不再需要反复轮询 `bg_output`；插件卸载时唤醒所有等待中的调用，不再挂到自身超时。

### 修复与改进

- **同步等待的命令子命令先弹 toast 反馈**：`/bg cancel`（abort 最多 10s）、`/bg send|resume` 对已结束任务（并发槽等待最多 15s）此前在 hook 里静默等待、界面无任何反馈；现在执行前先弹"正在取消/正在恢复"toast。
- **后台任务重试不再被 TTL 误杀**：同模型重试重新入队时重置 TTL 锚点（此前保留首次启动时间，任务运行约 30 分钟后遇可重试错误会被 stale-prune 以"排队超时"为由静默取消，错误原因误导排查）。
- **`bg_send` 对已结束任务的续跑等待有界可取消**：此前模型组并发槽被长任务占满时，续跑的并发等待无限挂起且无法取消（任务已终态，`/bg cancel` 对其无效），工具调用卡死整个回合。现在等待上限 15 秒，超时返回可操作的错误提示；等待期间取消该任务会立即解除挂起。
- **/split 启动失败的子任务级联跳过下游**：`manager.launch` 失败（如模型解析瞬时失败）时，此前依赖它的下游任务会在缺失上游输出的情况下照常启动；现在按"上游失败"级联跳过，汇总报告中该计划标注"启动失败"而非误导性的"未启动"。汇总报告彻底投递失败时会把报告全文写入插件日志兜底（SKIPPED 计划信息只此一份）。
- **Windows 兼容**：① `~\xxx.png` 形式的主目录路径此前被静默解析到项目目录下的 `~/` 子目录（识别与展开两处的反斜杠归一化时机不一致）；② UTF-8 BOM 的 `prism.jsonc`（Windows 记事本/PowerShell 5 默认保存格式）此前整体解析失败、全量回退默认配置——现在 BOM 在解析前剥离。
- **图片大小上限对齐最严格的提供商限制**：单图 8MB → 4MB（Anthropic 拒绝单图 >5MB），新增整批 base64 负载 16MB 上限（Gemini 限制整个 inline 请求 20MB，4 张上限图片 × base64 膨胀 ~33% 原本会超），超限保留能装下的图片、丢弃尾部；远程图片先读 `Content-Length` 预检，超大响应不再全量下载后才拒绝。文件大小与上下文占用无关（各提供商按分辨率计 token，4K 截图服务端缩放后约 3k tokens）。
- **`/bg output --full` 的 attach 提示 URL 改用 `127.0.0.1`**：Windows 上 `localhost` 可能优先解析为 IPv6 `::1`，服务端只绑 `127.0.0.1` 时提示的 URL 连不上；宿主传入的 serverUrl 仍原样透传。
- **子会话 abort 超时后晚到的失败不再无痕吞掉**：10 秒竞速超时后若 abort 请求随后失败，此前该 rejection 既不重试也无日志（`Promise.race` 吞掉），现在记录到插件日志。
- **视觉解读失败提示按真实原因区分**：此前手动解读（`vision_look`）的任何失败都统一兜底为"无可用视觉模型（请配置 vision.model）"——图片引用无效、子会话创建失败、模型无输出、超时等场景全部被误报为模型未配置（2026-08-22 事故：已正确配置并解析 `vision.model`，却因附件占位符无效而解读失败，用户被提示"去配置 vision.model"）。现在每种失败原因对应准确提示（"无可用模型"只在真的没有模型时出现）；无效图片引用属确定性失败，不再触发第二次重试。
- **`vision_look` 支持 `[Image N]` 附件占位符**：主模型转述附件时可能把客户端渲染的 `[Image N]` 占位符当参数传入，此前会被当作无效图片引用导致解读失败。现在全占位符参数自动等价 `last`（解读会话内最近的图片，即附件本身；定位不到时给出针对性引导）；占位符与真实引用混合时解读真实引用并附忽略说明。
- **内部清理**：移除只写不读的 `pendingByParent` 状态（及其过时注释）；`vision_look` 的 `images` 参数增加非空约束；配置加载中恒真的判空分支清理。

## [0.2.0-beta.0] - 2026-08-22

### 破坏性变更

- **移除 tmux pane 可视化**：整个 tmux 模块（pane 生命周期、attach 命令、布局）及相关 `tmux` 配置节。旧配置中的 `tmux` 节会被静默忽略，无需迁移。子任务可见性改为 toast + 完成通知 + `/bg output <id> --full` 的 `opencode attach` 提示。
- **移除 `vision.chatImages` 配置**：对话贴图不再自动解读（原机制依赖实验性 `messages.transform` hook，且解读结果不落会话历史）。改为手动路径——`vision_look` 工具与新增的 `/vision` 命令，结果均持久化在会话历史中。旧配置中的 `chatImages` 会被静默忽略。
- **对话贴图自动解读改为手动**：纯文本主模型收到用户贴图时，由主模型调用 `vision_look(images: ["last"], goal: ...)` 解读，或用户直接 `/vision last`。

### 新增

- **`vision_look` 支持 `goal` 关注点与 `"last"` 哨兵**：goal 让解读只回答关注点相关内容（信噪比更高、占用上下文更少）；`"last"` 表示"本会话最近的一张图片"，解决纯文本主模型无法引用贴图 URL 的问题。
- **`/vision` 命令**：`/vision <路径/URL ... | last> [--goal <关注点>]`，插件原生执行，解读结果直接注入对话。
- **`/bg resume <task_id> <追问>`**：在已结束任务的子会话里继续追问，保留其上下文，无需重新启动任务。

### 修复与改进

- **安全**：图片 URL 仅允许 http(s) 协议经 fetch 获取（拒绝 `file://` 等——Bun 的 fetch 会直接读取本地文件，构成任意文件读取面）；`data:` URL 纳入 8MB 上限与魔数校验，声称的 mime 不再被信任。
- **JSONC 配置解析**：修复字符串值含 `, ]` / `, }` 时被尾逗号剥离逻辑静默破坏的问题。
- **后台任务**：排队超 30 分钟自动取消，**运行中**超时只告警不取消（长任务不再被误杀）；任务结果改为全部 assistant 文本按序拼接（上限 20k，多轮子会话的中间结论不再丢失）；汇总通知投递失败自动重试一次。
- **/split**：上游子任务失败时，依赖它的下游级联标记 `SKIPPED`（不再基于空结果继续执行），汇总报告展示跳过原因。
- **视觉解读超时治理**：单次解读超时降为 60s，且超时不再触发第二次重试（最坏阻塞从 240s 降至 60s）。
- **跨平台**：Windows 日志路径改用 `%APPDATA%`；`.\relative.png` 反斜杠相对路径可识别；日志文件超 10MB 自动轮转。
- **其他**：`/bg output --full` 的 attach 提示在服务端启用密码认证时附加说明；删除大量历史遗留死代码（provider 优先级链、chat.message hook、孤儿类型等）。

## [0.1.0-beta.0] - 2026-08-13

首个 beta。

- 视觉自动解读：工具输出图片附件自动解读并追加；对话贴图两阶段注入（`messages.transform`）
- 后台并行引擎：`/bg` 命令 + `bg_spawn/bg_output/bg_cancel` 工具，按 provider/model 并发信号量、toast 进度、会话内汇总通知
- `/split` 任务拆分：规划器产出依赖图（DAG），按层并发调度
- tmux pane 可视化：每个后台任务一个 pane，实时显示子 agent TUI
- PromptGate 内部消息注入闸门：按会话预留、同文本去重、忙时等待 settle
