{
  "name": "opencode-prism",
  "version": "0.1.0",
  "description": "Prism: OpenCode plugin for vision interpretation, parallel background agents, task splitting, and tmux pane visualization",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun --external @opencode-ai/plugin --external zod"
  },
  "engines": {
    "opencode": ">=1.15.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.18.18",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
